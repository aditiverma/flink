
---
machine:
  java:
    version: oraclejdk8
  environment:
    LD_LIBRARY_PATH: /usr/local/lib

dependencies:
  cache_directories:
    - "~/.m2/"
  pre:
    - git clone https://github.com/BranchMetrics/deploy.git
    - cp deploy/maven/settings.xml $HOME/.m2/
  override:
    - mvn -B clean compile
  post:

test:
  override:
    - mvn -B clean package

deployment:
  staging:
    branch: branch_1.3.0
    commands:
      - mvn -DscmCommentPrefix="[ci skip]" -B clean deploy

  production:
    branch: branch_prod
    commands:
      - mvn -DscmCommentPrefix="[ci skip]" -B clean release:clean release:prepare release:perform
